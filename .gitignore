# Secrets — NEVER commit
.env
.env.*
!.env.example
!.env.local.example
.supabase-credentials.txt
.supabase-credentials.txt.bak
*credentials*.txt
*credentials*.txt.bak
*credentials*.json
# Sentry source-map upload token (set on Vercel; .env.* glob above already
# covers it, but call it out explicitly so future maintainers know)
.env.sentry-build-plugin
.sentryclirc

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Step-4 harness scratch artifacts (re-ingest candidates/quarantine/reports)
backend/scratch/
.venv-claude/
venv/
*.egg-info/
.pytest_cache/

# Node / Next.js
node_modules/
.next/
out/
dist/

# OS / IDE
.DS_Store
.vscode/
.idea/

# Local uploads / exports / logs
backend/uploads/
backend/exports/
exports/
backend/cache/
backend/logs/
*.log

# Benchmark run outputs (reproducible from manifest)
benchmarks/results/
